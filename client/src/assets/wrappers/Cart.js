import styled from "styled-components";

const Wrapper = styled.div`
  display: grid;
  align-items: center;
  h2 {
    margin-top: 15rem;
    text-align: center;
    opacity: 0.4;
  }
`;
export default Wrapper;
